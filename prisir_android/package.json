{
  "name": "prisirai-remote",
  "version": "0.2.0",
  "description": "PrisirAI Android remote control - LAN pairing + WebView",
  "scripts": {
    "sync": "cap sync android",
    "open": "cap open android",
    "build": "cap sync android && cd android && gradlew assembleDebug"
  },
  "dependencies": {
    "@capacitor/android": "^6.2.0",
    "@capacitor/core": "^6.2.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.2.0"
  }
}
